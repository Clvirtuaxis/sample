<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="title">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="description">
    <%= f.label :description %>
    <%= f.text_area :description, rows: "6", cols: "45", class: "form" %>
  
   <br><br>
  
    <%= f.label :image_url %>
    <%= f.text_field :image_url, class: "form" %>
  

  
    <%= f.label :price_in_cents %><br>
    <%= f.text_field :price_in_cents, class: "form" %>
  </div>

  <div class="actions">
    <%= f.submit nil, class: "btn_actions" %>
  </div>

<% end %>
